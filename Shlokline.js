import React from 'react';
import { StyleSheet, View } from 'react-native';
import {Text} from 'react-native-paper';


export default ShlokLine = ({shline}) => (<Text style={{fontSize: 18}}>{shline}</Text>);
